#include <stdio.h>
#include <time.h>


int main()
{
  const int HIGH = 11; 		// Easy range edit for high
  const int LOW = 0;		// Easy range edit for low
  int flag = 0;

	do {
		printf("Test");
	} while (flag == 1)
  
}
